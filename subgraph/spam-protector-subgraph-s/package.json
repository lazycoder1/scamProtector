{
    "name": "spam-protector",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --studio spamprotector-some-chain",
        "create-local": "graph create --node http://localhost:8020/ spam-protector",
        "remove-local": "graph remove --node http://localhost:8020/ spam-protector",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 spam-protector",
        "test": "graph test"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.82.0",
        "@graphprotocol/graph-ts": "0.35.1"
    },
    "devDependencies": {
        "matchstick-as": "0.5.0"
    }
}
